export module skbase.base;

import std;

export using ushort = unsigned short;
export using byte = unsigned char;
export using uint = unsigned int;

export using ms_t = std::chrono::milliseconds;
export using sec_t = std::chrono::seconds;
export using min_t = std::chrono::minutes;
